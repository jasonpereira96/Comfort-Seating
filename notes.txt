Comfort Seating font could be changed to something more cursive

Put a SHOP button below Comfort Seating

change our services to our values

can add testimonies on the home page? or may be no

sup quality - star/gavel
built for comfortable - tag_faces
multipurpose open_with

how to make div clickable - done

to write
each product description
about us
addresses of the shops
personalized para
testimonies reframe
values paras
fix footer and footer menu


leave the map

$ git add .
$ git status // to see what changes are going to be commited
$ git commit -m 'Some descriptive commit message'
$ git push origin master

$ git checkout gh-pages // go to the gh-pages branch
$ git rebase master // bring gh-pages up to date with master
$ git push origin gh-pages // commit the changes
$ git checkout master // return to the master branch
